spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/spring_batch_tutorial
    username: spring_batch_tutorial
    password: spring_batch_tutorial
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

logging:
  level:
    com.edgareldy.springbatchtutorial: DEBUG
